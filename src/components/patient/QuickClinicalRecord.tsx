import React,{useState} from 'react';
import {ClipboardPlus,X,Mic,Square,Play,Pause} from 'lucide-react';
import { uploadClinicalMedia } from '../../services/mediaStorage';
import {Patient,ProgressNote} from '../../types/clinical';
import {useApp} from '../../context/AppContext';

interface Props{patient:Patient;onClose:()=>void;}
export const QuickClinicalRecord:React.FC<Props>=({patient,onClose})=>{
 const{updatePatient,showToast,auth}=useApp();const[text,setText]=useState('');const[type,setType]=useState('Quick Clinical Record');
 const[recording,setRecording]=useState(false);const[audioBlob,setAudioBlob]=useState<Blob|null>(null);const[audioUrl,setAudioUrl]=useState('');const[duration,setDuration]=useState(0);const recorderRef=React.useRef<MediaRecorder|null>(null);const chunksRef=React.useRef<Blob[]>([]);const startedRef=React.useRef(0);const timerRef=React.useRef<number|null>(null);
 const startRecording=async()=>{try{if(!navigator.mediaDevices?.getUserMedia||typeof MediaRecorder==='undefined'){showToast('Audio recording is not supported on this device/browser.','error');return;}const stream=await navigator.mediaDevices.getUserMedia({audio:true});const recorder=new MediaRecorder(stream);chunksRef.current=[];startedRef.current=Date.now();recorder.ondataavailable=e=>{if(e.data.size)chunksRef.current.push(e.data);};recorder.onstop=()=>{stream.getTracks().forEach(t=>t.stop());const blob=new Blob(chunksRef.current,{type:recorder.mimeType||'audio/webm'});setAudioBlob(blob);setAudioUrl(URL.createObjectURL(blob));setDuration(Math.round((Date.now()-startedRef.current)/1000));};recorderRef.current=recorder;recorder.start();setRecording(true);timerRef.current=window.setInterval(()=>setDuration(Math.round((Date.now()-startedRef.current)/1000)),500);}catch(error){console.error(error);showToast('Microphone permission was denied or unavailable.','error');}};
 const stopRecording=()=>{recorderRef.current?.stop();recorderRef.current=null;setRecording(false);if(timerRef.current!==null){window.clearInterval(timerRef.current);timerRef.current=null;}};
 const clearRecording=()=>{if(recording)stopRecording();if(audioUrl)URL.revokeObjectURL(audioUrl);setAudioBlob(null);setAudioUrl('');setDuration(0);};
 const save=async()=>{const value=text.trim();if(!value&&!audioBlob)return;const now=new Date();const id=`quick-${Date.now()}`;let savedAudioUrl:string|undefined;let audioStoragePath:string|undefined;if(audioBlob){const path=`patients/${patient.id}/quick-records/${id}.webm`;const uploaded=await uploadClinicalMedia(audioBlob,path);savedAudioUrl=uploaded.url;if(uploaded.cloud)audioStoragePath=path;}const note:ProgressNote={id,date:now.toISOString().split('T')[0],time:now.toTimeString().slice(0,5),author:auth.userName||'Physician',type,subjective:'',objective:'',assessment:value,plan:value||'Audio clinical record',audioUrl:savedAudioUrl,audioStoragePath,audioDurationSeconds:duration,updatedAt:now.toISOString()};updatePatient(patient.id,{progressNotes:[note,...(patient.progressNotes||[])]});showToast('Clinical record saved to Progress Notes and Timeline.','success');clearRecording();onClose();};
 return <div className="fixed inset-0 z-[110] bg-slate-950/60 backdrop-blur-sm p-3 flex items-end sm:items-center justify-center"><div className="w-full max-w-lg rounded-3xl bg-white dark:bg-[#0E1626] border border-slate-200 dark:border-slate-800 shadow-2xl p-5"><div className="flex items-center justify-between mb-4"><div><h2 className="text-base font-extrabold text-slate-900 dark:text-white flex items-center gap-2"><ClipboardPlus className="w-5 h-5 text-cyan-500"/>Quick Clinical Record</h2><p className="text-[11px] text-slate-400">Saved as a dated Progress Note and shown in the patient timeline.</p></div><button onClick={onClose} className="p-2 rounded-xl hover:bg-slate-100 dark:hover:bg-slate-800"><X className="w-4 h-4"/></button></div><label className="block text-xs font-bold text-slate-600 dark:text-slate-300">Record type<select value={type} onChange={e=>setType(e.target.value)} className="mt-1 w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-900 px-3 py-2.5 text-sm"><option>Quick Clinical Record</option><option>Rounding Update</option><option>Clinical Event</option><option>Consultation Update</option><option>Important Observation</option></select></label><div className="mt-3 p-3 rounded-2xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-900"><div className="flex items-center justify-between gap-2"><div><div className="text-xs font-bold text-slate-700 dark:text-slate-200">Voice note</div><div className="text-[10px] text-slate-400">{recording?'Recording…':audioBlob?`Recorded ${duration}s`:'Optional audio recording'}</div></div><div className="flex gap-2">{!recording?<button type="button" onClick={startRecording} className="px-3 py-2 rounded-xl bg-cyan-500 text-white text-xs font-bold flex items-center gap-1"><Mic className="w-4 h-4"/>Record</button>:<button type="button" onClick={stopRecording} className="px-3 py-2 rounded-xl bg-rose-500 text-white text-xs font-bold flex items-center gap-1"><Square className="w-4 h-4"/>Stop</button>}{audioBlob&&<button type="button" onClick={clearRecording} className="px-3 py-2 rounded-xl border border-slate-200 dark:border-slate-700 text-xs font-bold">Clear</button>}</div></div>{audioUrl&&<audio controls src={audioUrl} className="w-full mt-3"/></div><div className="mt-3 p-3 rounded-2xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-900"><div className="flex items-center justify-between gap-2"><div><div className="text-xs font-bold text-slate-700 dark:text-slate-200">Voice note</div><div className="text-[10px] text-slate-400">{recording?'Recording…':audioBlob?`Recorded ${duration}s`:'Optional audio recording'}</div></div><div className="flex gap-2">{!recording?<button type="button" onClick={startRecording} className="px-3 py-2 rounded-xl bg-cyan-500 text-white text-xs font-bold flex items-center gap-1"><Mic className="w-4 h-4"/>Record</button>:<button type="button" onClick={stopRecording} className="px-3 py-2 rounded-xl bg-rose-500 text-white text-xs font-bold flex items-center gap-1"><Square className="w-4 h-4"/>Stop</button>}{audioBlob&&<button type="button" onClick={clearRecording} className="px-3 py-2 rounded-xl border border-slate-200 dark:border-slate-700 text-xs font-bold">Clear</button>}</div></div>{audioUrl&&<audio controls src={audioUrl} className="w-full mt-3"/></div><textarea autoFocus value={text} onChange={e=>setText(e.target.value)} placeholder="Document the important clinical event, observation, response, or update…" className="mt-3 w-full min-h-[170px] resize-y rounded-2xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-900 px-4 py-3 text-sm outline-none focus:border-cyan-500 text-slate-900 dark:text-white"/><div className="flex justify-end gap-2 mt-4"><button onClick={onClose} className="px-4 py-2.5 rounded-xl text-xs font-bold border border-slate-200 dark:border-slate-700">Cancel</button><button disabled={!text.trim()&&!audioBlob||recording} onClick={save} className="px-5 py-2.5 rounded-xl bg-cyan-500 disabled:opacity-40 text-white text-xs font-bold">Save Record</button></div></div></div>;
};
