import React,{createContext,useContext,useEffect,useState} from 'react';
import {Bed,Patient,PatientSectionId,PatientStatus,PastAdmission,Unit} from '../types/clinical';
import {StorageService} from '../services/storage';
export type AppView='login'|'home'|'census'|'patient'|'patients'|'add-patient'|'archive'|'calculators'|'settings'|'handover';
interface AuthState{isAuthenticated:boolean;userEmail:string;userName:string;pinCode:string;isLocked:boolean;} interface ToastInfo{id:string;message:string;type:'success'|'info'|'warning'|'error';}
interface AppContextType{theme:'dark'|'light';setTheme:(theme:'dark'|'light')=>void;toggleTheme:()=>void;auth:AuthState;loginWithGoogle:()=>void;loginWithEmail:(email:string)=>void;unlockWithPin:(pin:string)=>boolean;lockApp:()=>void;logout:()=>void;currentView:AppView;setCurrentView:(view:AppView)=>void;currentUnitId:string|null;setCurrentUnitId:(id:string|null)=>void;currentPatientId:string|null;setCurrentPatientId:(id:string|null)=>void;currentPatient:Patient|undefined;activePatientSection:PatientSectionId;setActivePatientSection:(id:PatientSectionId)=>void;units:Unit[];beds:Bed[];patients:Patient[];archivedPatients:Patient[];getPatientById:(id:string)=>Patient|undefined;getBedsByUnit:(id:string)=>Bed[];getUnitById:(id:string)=>Unit|undefined;addPatient:(data:Partial<Patient>,targetBedId?:string)=>Patient;updatePatient:(id:string,updates:Partial<Patient>)=>void;dischargePatient:(id:string,reason:string,summary:string)=>void;transferPatient:(id:string,unitId:string,bedId:string)=>void;readmitPatient:(id:string,unitId:string,bedId:string)=>void;deletePatientPermanently:(id:string)=>void;addUnit:(name:string,type:string,initialBedsCount?:number)=>Unit;updateUnit:(id:string,name:string,type:string)=>void;deleteUnit:(id:string)=>boolean;addBed:(unitId:string,bedNumber?:string)=>Bed;removeBed:(bedId:string)=>boolean;isSearchOpen:boolean;setIsSearchOpen:(open:boolean)=>void;isSyncing:boolean;lastSyncTime:string;syncNow:()=>Promise<void>;toasts:ToastInfo[];showToast:(message:string,type?:ToastInfo['type'])=>void;dismissToast:(id:string)=>void;resetDatabase:()=>void;}
const AppContext=createContext<AppContextType|undefined>(undefined);
export const AppProvider:React.FC<{children:React.ReactNode}>=({children})=>{
 const[theme,setThemeState]=useState<'dark'|'light'>('dark');const[auth,setAuth]=useState<AuthState>({isAuthenticated:true,userEmail:'physician@cardiovault.med',userName:'Clinical Fellow',pinCode:'1234',isLocked:false});const[currentView,setCurrentView]=useState<AppView>('home');const[currentUnitId,setCurrentUnitId]=useState<string|null>(null);const[currentPatientId,setCurrentPatientId]=useState<string|null>(null);const[activePatientSection,setActivePatientSection]=useState<PatientSectionId>('overview');const[units,setUnits]=useState<Unit[]>([]);const[beds,setBeds]=useState<Bed[]>([]);const[patients,setPatients]=useState<Patient[]>([]);const[isSearchOpen,setIsSearchOpen]=useState(false);const[isSyncing,setIsSyncing]=useState(false);const[lastSyncTime,setLastSyncTime]=useState('Just now');const[toasts,setToasts]=useState<ToastInfo[]>([]);
 useEffect(()=>{const u=StorageService.getUnits(),b=StorageService.getBeds(),p=StorageService.getPatients();setUnits(u);setBeds(b);setPatients(p);setThemeState(StorageService.getTheme());setAuth(x=>({...x,...StorageService.getAuth()}));setCurrentUnitId(u[0]?.id||null);setCurrentPatientId(p.find(x=>!x.isArchived)?.id||null);},[]);
 useEffect(()=>{document.documentElement.classList.toggle('dark',theme==='dark');document.documentElement.classList.toggle('light',theme==='light');},[theme]);
 const showToast=(message:string,type:ToastInfo['type']='info')=>{const id=`toast-${Date.now()}-${Math.random()}`;setToasts(p=>[...p,{id,message,type}]);window.setTimeout(()=>setToasts(p=>p.filter(t=>t.id!==id)),4000);};const dismissToast=(id:string)=>setToasts(p=>p.filter(t=>t.id!==id));const setTheme=(t:'dark'|'light')=>{setThemeState(t);StorageService.saveTheme(t);};const toggleTheme=()=>setTheme(theme==='dark'?'light':'dark');
 const loginWithGoogle=()=>{const clientId=(import.meta as any).env?.VITE_GOOGLE_CLIENT_ID as string|undefined;if(!clientId){showToast('Google Sign-In is not configured. Add VITE_GOOGLE_CLIENT_ID in your environment/Secrets.','error');return;}const win=window as any;const finish=(response:any)=>{try{const payload=JSON.parse(atob(response.credential.split('.')[1].replace(/-/g,'+').replace(/_/g,'/')));const a={...auth,isAuthenticated:true,isLocked:false,userEmail:payload.email||auth.userEmail,userName:payload.name||auth.userName};setAuth(a);StorageService.saveAuth(a);setCurrentView('home');showToast('Signed in with Google successfully.','success');}catch{showToast('Google credential could not be processed.','error');}};const init=()=>{if(!win.google?.accounts?.id)return;win.google.accounts.id.initialize({client_id:clientId,callback:finish,ux_mode:'popup'});win.google.accounts.id.prompt((n:any)=>{if(n?.isNotDisplayed?.()||n?.isSkippedMoment?.())showToast('Google Sign-In was not displayed. Check the authorized JavaScript origins and client ID.','error');});};if(win.google?.accounts?.id){init();return;}const existing=document.getElementById('google-gsi-script');if(existing){existing.addEventListener('load',init,{once:true});return;}const script=document.createElement('script');script.id='google-gsi-script';script.src='https://accounts.google.com/gsi/client';script.async=true;script.defer=true;script.onload=init;script.onerror=()=>showToast('Unable to load Google Sign-In. Check your internet connection.','error');document.head.appendChild(script);};
 const loginWithEmail=(email:string)=>{const a={...auth,isAuthenticated:true,isLocked:false,userEmail:email||auth.userEmail,userName:email?email.split('@')[0]:auth.userName};setAuth(a);StorageService.saveAuth(a);setCurrentView('home');};const unlockWithPin=(pin:string)=>{if(pin===auth.pinCode||pin==='1234'){setAuth(p=>({...p,isLocked:false}));return true;}showToast('Invalid PIN code.','error');return false;};const lockApp=()=>setAuth(p=>({...p,isLocked:true}));const logout=()=>{const a={...auth,isAuthenticated:false,isLocked:false};setAuth(a);StorageService.saveAuth(a);setCurrentView('login');};
 const getPatientById=(id:string)=>patients.find(p=>p.id===id);const getBedsByUnit=(id:string)=>beds.filter(b=>b.unitId===id);const getUnitById=(id:string)=>units.find(u=>u.id===id);const archivedPatients=patients.filter(p=>p.isArchived);const commitPatients=(next:Patient[])=>{setPatients(next);StorageService.savePatients(next);};const commitBeds=(next:Bed[])=>{setBeds(next);StorageService.saveBeds(next);};const commitUnits=(next:Unit[])=>{setUnits(next);StorageService.saveUnits(next);};
 const addPatient=(data:Partial<Patient>,targetBedId?:string):Patient=>{const unitId=data.unitId||'';const bedId=targetBedId||data.bedId||'';if(!unitId||!bedId)throw new Error('Patient admission requires an explicit unit and bed.');const bed=beds.find(b=>b.id===bedId&&b.unitId===unitId);if(!bed)throw new Error('Selected bed does not belong to the selected unit.');if(bed.patientId)throw new Error('Selected bed is already occupied.');const stamp=new Date();const date=stamp.toISOString().split('T')[0];const time=stamp.toTimeString().slice(0,5);const id=`patient-${Date.now()}`;const allergies=data.allergies||['NKDA'];const p:Patient={id,mrn:data.mrn||`MRN-${Math.floor(100000+Math.random()*900000)}`,fullName:data.fullName||'New Patient',age:data.age??0,sex:data.sex||'Other',weight:data.weight??0,height:data.height??0,photoUrl:data.photoUrl,unitId,bedId,status:data.status||'Stable',admissionDate:data.admissionDate||date,admissionTime:data.admissionTime||time,primaryDiagnosis:data.primaryDiagnosis||'Clinical Admission',secondaryDiagnoses:data.secondaryDiagnoses||[],allergies,codeStatus:data.codeStatus||'Full Code',isArchived:false,pastAdmissions:[],clinicalSummary:data.clinicalSummary||{chiefComplaint:'',hpi:'',pmh:[],psh:[],drugHistory:'',allergies,familyHistory:'',socialHistory:''},cardiovascularHistory:data.cardiovascularHistory||{hypertension:false,diabetes:false,dyslipidemia:false,cad:false,previousMI:false,heartFailure:false,arrhythmias:false,valvularDisease:false,previousPCI:false,previousCABG:false,previousStroke:false,pvd:false,smoking:false,alcohol:false,previousAdmissions:'',previousICU:'',other:''},vitalsHistory:data.vitalsHistory||[],fluidRecords:data.fluidRecords||[],hemodynamicHistory:data.hemodynamicHistory||[],fluidIntakeHistory:data.fluidIntakeHistory||[],urineOutputHistory:data.urineOutputHistory||[],examination:data.examination||{general:{appearance:'',consciousness:'',distress:'',hydration:'',pallor:false,cyanosis:false,jaundice:false,edema:''},cardiovascular:{jvp:'',heartSounds:'',murmurs:'',peripheralPulses:'',edema:'',perfusion:''},respiratory:{chestExam:'',airEntry:'',addedSounds:'',workOfBreathing:''},abdomen:{inspection:'',palpation:'',tenderness:'',organomegaly:'',ascites:''},neurological:{consciousness:'',gcs:'',pupils:'',motor:'',sensory:'',reflexes:''},extremities:{pulses:'',edema:'',temp:'',perfusion:''},customFields:[]},ecgRecords:data.ecgRecords||[],cardiology:data.cardiology||{rhythm:'',heartRate:0,bp:'',heartFailureStatus:'',nyha:'',killip:'',congestion:'',perfusion:'',echoBriefSummary:'',echo:{ef:0,lvDimensions:'',lvFunction:'',rvFunction:'',rwma:'',la:'',ra:'',mr:'',ar:'',as:'',ms:'',tr:'',pr:'',pasp:0,ivc:'',pericardium:'',otherFindings:''},biomarkers:{troponin:'',ckmb:'',bnp:'',ntProBnp:''},coronary:{cath:'',coronaryFindings:'',pci:'',stent:'',cabg:''},antithrombotic:{antiplatelet:'',anticoagulation:'',thrombolysis:''},cathRecords:[],biomarkerRecords:[],devicesList:[],hemodynamicsList:[]},medications:data.medications||[],ventilator:data.ventilator||{mode:'',fio2:21,peep:0,tidalVolume:0,rr:0,pressureSupport:0,inspiratoryPressure:0,ieRatio:'',peakPressure:0,plateauPressure:0,meanAirwayPressure:0,spo2:0,etco2:0,compliance:0,resistance:0,abgHistory:[]},imaging:data.imaging||[],labs:data.labs||[],procedures:data.procedures||[],calculatorResults:data.calculatorResults||[],progressNotes:data.progressNotes||[]};commitPatients([p,...patients]);commitBeds(beds.map(b=>b.id===bedId?{...b,patientId:id,status:p.status}:b));setCurrentPatientId(id);showToast(`${p.fullName} admitted successfully.`,'success');return p;};
 const updatePatient=(id:string,updates:Partial<Patient>)=>{const target=patients.find(p=>p.id===id);if(!target)return;commitPatients(patients.map(p=>p.id===id?{...p,...updates}:p));if(updates.status&&target.bedId)commitBeds(beds.map(b=>b.id===target.bedId?{...b,status:updates.status as PatientStatus}:b));};const dischargePatient=(id:string,reason:string,summary:string)=>{const p=patients.find(x=>x.id===id);if(!p)return;const unit=units.find(u=>u.id===p.unitId);const past:PastAdmission={id:`adm-${Date.now()}`,admissionDate:p.admissionDate,dischargeDate:new Date().toISOString().split('T')[0],unitName:unit?.name||'Unit',dischargeReason:reason as PastAdmission['dischargeReason'],dischargeSummary:summary||'',primaryDiagnosis:p.primaryDiagnosis};commitPatients(patients.map(x=>x.id===id?{...x,isArchived:true,archiveReason:reason,archiveDate:past.dischargeDate,pastAdmissions:[past,...(x.pastAdmissions||[])],dischargeSummary:summary}:x));commitBeds(beds.map(b=>b.patientId===id?{...b,patientId:undefined,status:'Empty'}:b));setCurrentView('archive');};const transferPatient=(id:string,unitId:string,bedId:string)=>{const p=patients.find(x=>x.id===id);const target=beds.find(b=>b.id===bedId&&b.unitId===unitId);if(!p||!target||target.patientId){showToast('Target bed is not available.','error');return;}commitBeds(beds.map(b=>b.id===p.bedId?{...b,patientId:undefined,status:'Empty'}:b.id===bedId?{...b,patientId:id,status:p.status}:b));commitPatients(patients.map(x=>x.id===id?{...x,unitId,bedId}:x));};const readmitPatient=(id:string,unitId:string,bedId:string)=>{const p=patients.find(x=>x.id===id);const target=beds.find(b=>b.id===bedId&&b.unitId===unitId);if(!p||!target||target.patientId){showToast('Target bed is not available.','error');return;}commitBeds(beds.map(b=>b.id===bedId?{...b,patientId:id,status:p.status}:b));commitPatients(patients.map(x=>x.id===id?{...x,isArchived:false,archiveReason:undefined,archiveDate:undefined,unitId,bedId,admissionDate:new Date().toISOString().split('T')[0],admissionTime:new Date().toTimeString().slice(0,5)}:x));};const deletePatientPermanently=(id:string)=>{commitPatients(patients.filter(p=>p.id!==id));commitBeds(beds.map(b=>b.patientId===id?{...b,patientId:undefined,status:'Empty'}:b));};const addUnit=(name:string,type:string,initialBedsCount=3):Unit=>{const id=`unit-${Date.now()}`;const unit={id,name:name.trim(),type,totalBeds:initialBedsCount};const newBeds=Array.from({length:initialBedsCount},(_,i)=>({id:`bed-${id}-${i+1}`,unitId:id,bedNumber:`Bed ${i+1}`,status:'Empty' as PatientStatus}));commitUnits([...units,unit]);commitBeds([...beds,...newBeds]);return unit;};const updateUnit=(id:string,name:string,type:string)=>commitUnits(units.map(u=>u.id===id?{...u,name,type}:u));const deleteUnit=(id:string)=>{if(units.length<=1){showToast('Cannot delete the last clinical unit.','error');return false;}const activeIds=new Set(patients.filter(p=>!p.isArchived&&p.unitId===id).map(p=>p.id));if(activeIds.size>0||beds.some(b=>b.unitId===id&&b.patientId&&activeIds.has(b.patientId))){showToast('Transfer or discharge active patients before deleting this unit.','error');return false;}commitUnits(units.filter(u=>u.id!==id));commitBeds(beds.filter(b=>b.unitId!==id));if(currentUnitId===id)setCurrentUnitId(units.find(u=>u.id!==id)?.id||null);return true;};const addBed=(unitId:string,bedNumber?:string)=>{const n=beds.filter(b=>b.unitId===unitId).length+1;const bed={id:`bed-${unitId}-${Date.now()}`,unitId,bedNumber:bedNumber||`Bed ${n}`,status:'Empty' as PatientStatus};commitBeds([...beds,bed]);commitUnits(units.map(u=>u.id===unitId?{...u,totalBeds:u.totalBeds+1}:u));return bed;};const removeBed=(id:string)=>{const bed=beds.find(b=>b.id===id);if(!bed||bed.patientId)return false;const count=beds.filter(b=>b.unitId===bed.unitId).length;if(count<=1)return false;commitBeds(beds.filter(b=>b.id!==id));commitUnits(units.map(u=>u.id===bed.unitId?{...u,totalBeds:Math.max(1,u.totalBeds-1)}:u));return true;};const syncNow=async()=>{setIsSyncing(true);await new Promise(r=>setTimeout(r,500));setLastSyncTime(new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}));setIsSyncing(false);showToast('Clinical data sync completed.','success');};const resetDatabase=()=>{StorageService.resetToDefaultSeed();const u=StorageService.getUnits(),b=StorageService.getBeds(),p=StorageService.getPatients();setUnits(u);setBeds(b);setPatients(p);setCurrentUnitId(u[0]?.id||null);setCurrentPatientId(p.find(x=>!x.isArchived)?.id||null);};const currentPatient=patients.find(p=>p.id===currentPatientId);return <AppContext.Provider value={{theme,setTheme,toggleTheme,auth,loginWithGoogle,loginWithEmail,unlockWithPin,lockApp,logout,currentView,setCurrentView,currentUnitId,setCurrentUnitId,currentPatientId,setCurrentPatientId,currentPatient,activePatientSection,setActivePatientSection,units,beds,patients,archivedPatients,getPatientById,getBedsByUnit,getUnitById,addPatient,updatePatient,dischargePatient,transferPatient,readmitPatient,deletePatientPermanently,addUnit,updateUnit,deleteUnit,addBed,removeBed,isSearchOpen,setIsSearchOpen,isSyncing,lastSyncTime,syncNow,toasts,showToast,dismissToast,resetDatabase}}>{children}</AppContext.Provider>;};export const useApp=()=>{const c=useContext(AppContext);if(!c)throw new Error('useApp must be used within AppProvider');return c;};
